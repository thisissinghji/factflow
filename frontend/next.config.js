/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains:[
      'www.vaidikyatra.org',
      'lh3.googleusercontent.com',
      'images.idgesg.net'
    ]
  }
}

module.exports = nextConfig
